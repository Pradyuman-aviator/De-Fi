{
  "name": "defi-arena-contracts",
  "version": "1.0.0",
  "description": "Multi-Agent DeFi Strategy Arena - Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:somnia": "hardhat run scripts/deploy.js --network somnia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.0"
  }
}